pipeline {
    agent any 
    stages {
        stage('Test stage') {
            steps {
                echo "${JOB_NAME}"
            }
        }
        stage('Return downstream info') {
            steps {
                echo "Jenkins URL is - ${JENKINS_URL}"
            }
        }
    }
    post {
        always { 
                echo "FirstFreestyleJob.getResult()"
                echo "SecondFreestyleJob.getResult()"
                
//                def ffj = build('FirstFreestyleJob', wait: true)
//                def result_ffj = b.result
                
//                def sfj = build('SecondFreestyleJob', wait: true)
//                def result_sfj = b.result        
        }
    }
}
